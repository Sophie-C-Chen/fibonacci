""" Installation instructions for fibonacci package in any directory. """
from setuptools import setup, find_packages

setup(
    name = "fibonacci",
    version = "0.1",
    packages=find_packages()
)
